# WebSocket URL for multiplayer game server (native only, not used on web)
# Replace XXX with your local IP when testing on native devices
# Find your IP: macOS/Linux -> `ifconfig | grep inet`, Windows -> `ipconfig`
# Example: EXPO_PUBLIC_WS_URL=ws://192.168.1.100:3001
EXPO_PUBLIC_WS_URL=ws://192.168.1.XXX:3001
